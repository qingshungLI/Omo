# Recallo V3 直接视觉模型选型状态

日期：2026-07-23

## 当前结论

`NO-GO / insufficient_evidence`

产品主模型已经锁定为：

- 候选 ID：`qwen3.7-plus-vision`
- 实际模型版本：`qwen3.7-plus-2026-05-26`
- 模式：非思考、直接视觉输入、结构化 JSON 输出

后续 benchmark 用于验证该指定主模型是否通过质量门，并选择修复/降级模型；不会因为其他模型更便宜而静默替换主模型。如果指定主模型未达标，结论保持 `NO-GO`。

这是数据与凭证尚未就绪，不是模型质量失败：

- 正式 Golden Set：0 / 60；
- 第二评审覆盖：0 / 12；
- `DASHSCOPE_API_KEY` / `QWEN_API_KEY`：未配置；
- `OPENAI_API_KEY`：未配置；
- 因此没有发生付费模型调用，也没有生成模型排名。

V2 生产生成管线未修改，V3 数据库纵切片未开始。

## 已完成

- 图片直接进入视觉模型，不使用 Apple Vision；
- `CaptureAnalysisInput` / `CaptureAnalysisOutput` 严格合同；
- 视觉模型自行返回证据文字、置信度与坐标，记忆点和题目引用 Evidence ID；
- 证据文字与人工校正全文的独立逐字核验；
- Prompt Injection、高风险确定性表述、无证据关键事实、选择题歧义等确定性门禁；
- 显式 provider/model 调用，不读取 V2 的隐式供应商选择；
- 最多一次 Schema 修复；
- 不保存原图、Base64 或完整原始模型响应；
- Token、延迟、成本、错误码、输出散列和盲评 CSV；
- 自动硬门、软指标、主模型与修复模型选择；
- 60 张真实集配额、授权、脱敏和 20% 双评审校验；
- 中文运行手册与安全预检。

## 当前候选

大陆直接视觉候选：

- `qwen3-vl-flash`
- `qwen3.6-flash`
- `qwen3.7-plus`
- 主模型实际调用固定为 `qwen3.7-plus-2026-05-26`

可选外部视觉对照：

- `gpt-5.6-terra`

DeepSeek 只保留为未来两阶段方案的文本诊断候选，不参与当前直接视觉自动选型。

## 验证

完整 `npm run check` 通过：

- 原后端测试：130 / 130；
- V2 测试：225 / 225；
- V3 模型选型测试：25 / 25；
- 合计：380 / 380。

预检确认默认 phase 为 `vision`，当前会列出三个 Qwen 视觉候选，并明确报告 Golden Set 和 Key 缺失；不带 `--execute` 时不会上传图片或产生费用。

## 下一人工输入

1. 提供 60 张经授权、脱敏的真实截图并完成标注；
2. 至少 12 张由第二位评审独立标注；
3. 为每张参与云端视觉评测的截图记录明确上传同意；
4. 在本机 `.env` 配置 DashScope/Qwen Key；
5. 先运行预检，再显式运行付费 benchmark 和人工盲评；
6. 只有最终报告为 `GO (quality_gate_passed)` 才进入 V3 后端纵切片。
