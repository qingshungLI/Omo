写提示词的重点不是堆叠复杂格式，而是把任务交代清楚。一个有效提示通常包含目标、背景、输入材料、输出格式、约束条件和判断标准。

当任务比较复杂时，不要一开始就要求模型直接给最终答案。更好的做法是先让模型复述任务、列出假设、拆解步骤，再逐步生成结果。这样可以更早发现理解偏差。

提示词不是一次写完就固定不变的文档，而是一种协作流程。用户需要根据模型输出不断补充边界、示例和反例。好的提示词往往来自几轮失败后的收敛。

如果模型经常输出空泛内容，通常不是因为它“不聪明”，而是任务缺少具体场景、评价标准或输入证据。此时应该先补上下文，而不是频繁更换模型。
